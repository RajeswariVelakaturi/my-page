<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sample Page</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(180deg, #000, #7b4c4c, #fff, #555, #000);
            height: 100vh;
        }

        /* NAVBAR */
        .navbar {
            width: 100%;
            background: red;
            padding: 15px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 100;
        }

        .logo {
            color: white;
            font-weight: bold;
            font-size: 22px;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            position: relative;
        }

        .nav-links a:hover {
            text-decoration: underline;
        }

        /* Login Button */
        .login-btn {
            padding: 6px 18px;
            border: 2px solid white;
            background: transparent;
            color: white;
            border-radius: 5px;
            cursor: pointer;
        }

        .login-btn:hover {
            background: white;
            color: red;
        }

        /* Content Area */
        .content {
            margin-top: 120px;
            height: 600px;
            background: white;
            border-radius: 10px;
            margin-left: 20px;
            margin-right: 20px;
        }
    </style>
</head>
<body>

    <div class="navbar">
        <div class="logo">Logo</div>

        <div class="nav-links">
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Services</a>
            <a href="#">Contact</a>
            <button class="login-btn">Login</button>
        </div>
    </div>

    <div class="content"></div>

</body>
</html>
